{
  "id": "phase-4",
  "title": "Phase 4: LLM API Mastery (Claude/OpenAI)",
  "description": "Master LLM APIs from first API call to production-ready applications with Claude and OpenAI.",
  "difficulty": "Intermediate",
  "topics": [
    {
      "id": "phase-4-topic-1",
      "title": "Your First API Call",
      "overview": "Get API keys, install SDKs, and make your first successful call to Claude or OpenAI.",
      "estimatedHours": 1.5,
      "difficulty": "Beginner",
      "detailedNotes": "# Your First API Call\n\nLet's make your first LLM API call!\n\n## Getting API Keys\n\n**Anthropic (Claude):** Go to console.anthropic.com, sign up, navigate to API Keys, create and save your key.\n\n**OpenAI (GPT):** Go to platform.openai.com, sign up, navigate to API Keys, create and save your key.\n\n## Installation\n\n```bash\npip install anthropic openai python-dotenv\n```\n\n## Setup .env File\n\n```\nANTHROPIC_API_KEY=sk-ant-...\nOPENAI_API_KEY=sk-...\n```\n\n## First Claude Call\n\n```python\nimport os\nfrom anthropic import Anthropic\nfrom dotenv import load_dotenv\n\nload_dotenv()\nclient = Anthropic(api_key=os.getenv('ANTHROPIC_API_KEY'))\n\nmessage = client.messages.create(\n    model=\"claude-3-5-sonnet-20241022\",\n    max_tokens=1024,\n    messages=[{\"role\": \"user\", \"content\": \"Hello, Claude!\"}]\n)\n\nprint(message.content[0].text)\n```\n\n## First OpenAI Call\n\n```python\nimport os\nfrom openai import OpenAI\nfrom dotenv import load_dotenv\n\nload_dotenv()\nclient = OpenAI(api_key=os.getenv('OPENAI_API_KEY'))\n\nresponse = client.chat.completions.create(\n    model=\"gpt-4\",\n    messages=[{\"role\": \"user\", \"content\": \"Hello, GPT!\"}]\n)\n\nprint(response.choices[0].message.content)\n```\n\n## Understanding Responses\n\nClaude returns a message object with content array and usage stats. OpenAI returns choices array with message content. Both track token usage for billing.\n\n## Error Handling\n\n```python\nfrom anthropic import APIError\n\ntry:\n    message = client.messages.create(...)\n    print(message.content[0].text)\nexcept APIError as e:\n    print(f\"API Error: {e}\")\n```\n\n## Common Issues\n\n1. Invalid API key - check .env file\n2. Rate limit - wait and retry\n3. Model not found - verify model name\n4. Timeout - increase timeout parameter",
      "keyConcepts": ["API keys", "SDK installation", "First API call", "Response structure", "Error handling"],
      "resources": [
        {"type": "article", "title": "Anthropic API Docs", "url": "https://docs.anthropic.com/", "source": "Anthropic"},
        {"type": "article", "title": "OpenAI API Docs", "url": "https://platform.openai.com/docs/", "source": "OpenAI"}
      ],
      "practiceExercise": "Make your first API call to both Claude and OpenAI. Print the response and token usage. Handle errors gracefully.",
      "prerequisites": ["phase-3-topic-3"],
      "nextTopics": ["phase-4-topic-2"]
    },
    {
      "id": "phase-4-topic-2",
      "title": "System Prompts & Personas",
      "overview": "Learn how system prompts shape model behavior and create effective personas for different use cases.",
      "estimatedHours": 2,
      "difficulty": "Intermediate",
      "detailedNotes": "# System Prompts & Personas\n\nSystem prompts define how the model behaves.\n\n## What is a System Prompt?\n\nInstructions that set the model's behavior, tone, and role before any user interaction.\n\n## Claude System Prompts\n\n```python\nmessage = client.messages.create(\n    model=\"claude-3-5-sonnet-20241022\",\n    max_tokens=1024,\n    system=\"You are a helpful Python tutor. Explain concepts clearly with code examples.\",\n    messages=[{\"role\": \"user\", \"content\": \"What are list comprehensions?\"}]\n)\n```\n\n## OpenAI System Messages\n\n```python\nresponse = client.chat.completions.create(\n    model=\"gpt-4\",\n    messages=[\n        {\"role\": \"system\", \"content\": \"You are a helpful Python tutor.\"},\n        {\"role\": \"user\", \"content\": \"What are list comprehensions?\"}\n    ]\n)\n```\n\n## Effective System Prompts\n\n**Define Role:** You are an expert Python developer with 10 years of experience.\n\n**Set Tone:** Be concise and technical. Avoid unnecessary explanations.\n\n**Specify Format:** Always provide code examples. Format responses as: 1) Brief explanation 2) Code example 3) Common pitfalls\n\n**Add Constraints:** Only use Python 3.10+ features, prefer type hints, follow PEP 8.\n\n## Example Personas\n\n**Code Reviewer:**\n```python\nsystem = \"\"\"You are a senior code reviewer. Analyze code for:\n- Bugs and errors\n- Performance issues\n- Security vulnerabilities\n- Best practices violations\nProvide specific, actionable feedback.\"\"\"\n```\n\n**Technical Writer:**\n```python\nsystem = \"\"\"You are a technical documentation writer.\nWrite clear, concise documentation with:\n- Overview\n- Parameters\n- Return values\n- Examples\n- Common errors\"\"\"\n```\n\n## Testing Personas\n\n```python\ndef test_persona(system_prompt, user_message):\n    message = client.messages.create(\n        model=\"claude-3-5-sonnet-20241022\",\n        max_tokens=1024,\n        system=system_prompt,\n        messages=[{\"role\": \"user\", \"content\": user_message}]\n    )\n    return message.content[0].text\n\nprompts = {\n    \"tutor\": \"You are a patient tutor.\",\n    \"expert\": \"You are a terse expert.\",\n    \"beginner\": \"Explain like I'm 5 years old.\"\n}\n\nfor name, prompt in prompts.items():\n    response = test_persona(prompt, \"What is recursion?\")\n    print(f\"{name}: {response}\\n\")\n```\n\n## Best Practices\n\n1. Be specific - vague prompts give inconsistent results\n2. Test variations - try different phrasings\n3. Include examples - show desired format\n4. Set boundaries - what NOT to do\n5. Iterate - refine based on outputs",
      "keyConcepts": ["System prompts", "Personas", "Role definition", "Tone setting", "Constraints"],
      "resources": [
        {"type": "article", "title": "Prompt Engineering Guide", "url": "https://www.promptingguide.ai/", "source": "Prompting Guide"}
      ],
      "practiceExercise": "Create 3 different personas (tutor, code reviewer, technical writer) and test them with the same question. Compare outputs.",
      "prerequisites": ["phase-4-topic-1"],
      "nextTopics": ["phase-4-topic-3"]
    },
    {
      "id": "phase-4-topic-3",
      "title": "Multi-turn Conversations",
      "overview": "Build stateful conversations by managing message history and context.",
      "estimatedHours": 2,
      "difficulty": "Intermediate",
      "detailedNotes": "# Multi-turn Conversations\n\nLLM APIs are stateless. You manage conversation history.\n\n## Message History Array\n\n```python\nconversation = [\n    {\"role\": \"user\", \"content\": \"What is Python?\"},\n    {\"role\": \"assistant\", \"content\": \"Python is a programming language...\"},\n    {\"role\": \"user\", \"content\": \"What are its main features?\"}\n]\n\nmessage = client.messages.create(\n    model=\"claude-3-5-sonnet-20241022\",\n    max_tokens=1024,\n    messages=conversation\n)\n```\n\n## Building a Chat Loop\n\n```python\ndef chat():\n    conversation = []\n    system_prompt = \"You are a helpful assistant.\"\n    \n    print(\"Chat started. Type 'quit' to exit.\")\n    \n    while True:\n        user_input = input(\"You: \")\n        if user_input.lower() == 'quit':\n            break\n        \n        conversation.append({\"role\": \"user\", \"content\": user_input})\n        \n        message = client.messages.create(\n            model=\"claude-3-5-sonnet-20241022\",\n            max_tokens=1024,\n            system=system_prompt,\n            messages=conversation\n        )\n        \n        assistant_message = message.content[0].text\n        conversation.append({\"role\": \"assistant\", \"content\": assistant_message})\n        \n        print(f\"Assistant: {assistant_message}\\n\")\n\nchat()\n```\n\n## Context Window Management\n\nModels have token limits. Trim old messages when needed.\n\n```python\ndef trim_conversation(messages, max_tokens=4000):\n    total_tokens = 0\n    trimmed = []\n    \n    for msg in reversed(messages):\n        msg_tokens = len(msg['content']) // 4\n        if total_tokens + msg_tokens > max_tokens:\n            break\n        trimmed.insert(0, msg)\n        total_tokens += msg_tokens\n    \n    return trimmed\n```\n\n## Conversation with Memory\n\n```python\nclass ChatBot:\n    def __init__(self, system_prompt):\n        self.system_prompt = system_prompt\n        self.conversation = []\n    \n    def send_message(self, user_message):\n        self.conversation.append({\"role\": \"user\", \"content\": user_message})\n        self.conversation = trim_conversation(self.conversation)\n        \n        message = client.messages.create(\n            model=\"claude-3-5-sonnet-20241022\",\n            max_tokens=1024,\n            system=self.system_prompt,\n            messages=self.conversation\n        )\n        \n        assistant_message = message.content[0].text\n        self.conversation.append({\"role\": \"assistant\", \"content\": assistant_message})\n        \n        return assistant_message\n    \n    def reset(self):\n        self.conversation = []\n\nbot = ChatBot(\"You are a Python tutor.\")\nprint(bot.send_message(\"What is a list?\"))\nprint(bot.send_message(\"How do I append to it?\"))\n```\n\n## Saving Conversations\n\n```python\nimport json\nfrom datetime import datetime\n\ndef save_conversation(conversation, filename=None):\n    if filename is None:\n        timestamp = datetime.now().strftime(\"%Y%m%d_%H%M%S\")\n        filename = f\"conversation_{timestamp}.json\"\n    \n    with open(filename, 'w') as f:\n        json.dump(conversation, f, indent=2)\n\ndef load_conversation(filename):\n    with open(filename, 'r') as f:\n        return json.load(f)\n```\n\n## Best Practices\n\n1. Always include full history for context\n2. Trim old messages to stay within limits\n3. Save important conversations\n4. Handle errors gracefully\n5. Test with long conversations",
      "keyConcepts": ["Message history", "Stateless APIs", "Context management", "Chat loops", "Conversation trimming"],
      "resources": [
        {"type": "article", "title": "Building Chatbots", "url": "https://docs.anthropic.com/claude/docs/building-with-claude", "source": "Anthropic"}
      ],
      "practiceExercise": "Build a chatbot that remembers conversation history. Add commands to save/load conversations and reset history.",
      "prerequisites": ["phase-4-topic-2"],
      "nextTopics": ["phase-4-topic-4"]
    },
    {
      "id": "phase-4-topic-4",
      "title": "Prompt Engineering Fundamentals",
      "overview": "Master techniques like chain-of-thought, few-shot examples, and output formatting.",
      "estimatedHours": 3,
      "difficulty": "Intermediate",
      "detailedNotes": "# Prompt Engineering Fundamentals\n\nHow you ask determines what you get.\n\n## Chain-of-Thought (CoT)\n\nAsk model to think step-by-step for better reasoning.\n\n**Without CoT:** \"What is 15% of 240?\" - May get wrong answer\n\n**With CoT:**\n```\nWhat is 15% of 240?\n\nLet's think step by step:\n1. Convert percentage to decimal\n2. Multiply by the number\n3. Show the calculation\n```\n\n## Few-Shot Examples\n\nShow examples of desired output format.\n\n```python\nprompt = \"\"\"Extract name and email from text.\n\nExamples:\nInput: \"Contact John at john@example.com\"\nOutput: {\"name\": \"John\", \"email\": \"john@example.com\"}\n\nInput: \"Reach out to Sarah (sarah.smith@company.com)\"\nOutput: {\"name\": \"Sarah\", \"email\": \"sarah.smith@company.com\"}\n\nNow extract from:\nInput: \"Email Mike at mike.jones@startup.io\"\nOutput:\"\"\"\n```\n\n## Output Formatting\n\n```python\nprompt = \"\"\"Analyze this code and respond in this exact format:\n\nBUGS:\n- [List any bugs]\n\nIMPROVEMENTS:\n- [List improvements]\n\nSECURITY:\n- [List security issues]\n\nCode:\n```python\ndef divide(a, b):\n    return a / b\n```\"\"\"\n```\n\n## Prompt Templates\n\n```python\nclass PromptTemplate:\n    def __init__(self, template):\n        self.template = template\n    \n    def format(self, **kwargs):\n        return self.template.format(**kwargs)\n\ncode_review_template = PromptTemplate(\"\"\"\nReview this {language} code:\n\n```{language}\n{code}\n```\n\nFocus on: {focus_area}\nProvide specific, actionable feedback.\"\"\")\n\nprompt = code_review_template.format(\n    language=\"python\",\n    code=\"def add(a, b): return a + b\",\n    focus_area=\"performance and type safety\"\n)\n```\n\n## Advanced Techniques\n\n**Self-Consistency:**\n```python\ndef get_consensus(prompt, n=3):\n    responses = []\n    for _ in range(n):\n        message = client.messages.create(\n            model=\"claude-3-5-sonnet-20241022\",\n            max_tokens=1024,\n            messages=[{\"role\": \"user\", \"content\": prompt}]\n        )\n        responses.append(message.content[0].text)\n    \n    from collections import Counter\n    return Counter(responses).most_common(1)[0][0]\n```\n\n**Prompt Chaining:**\n```python\n# Step 1: Extract key points\nstep1 = \"Summarize this article in 3 bullet points: {article}\"\n\n# Step 2: Expand each point\nstep2 = \"Expand on this point with examples: {point}\"\n\n# Step 3: Combine\nstep3 = \"Combine these expansions into a cohesive summary: {expansions}\"\n```\n\n## Best Practices\n\n1. Be specific - vague prompts give vague outputs\n2. Use examples - show desired format\n3. Break complex tasks - chain simpler prompts\n4. Test variations - A/B test prompts\n5. Iterate - refine based on results",
      "keyConcepts": ["Chain-of-thought", "Few-shot learning", "Output formatting", "Prompt templates", "Prompt chaining"],
      "resources": [
        {"type": "article", "title": "Prompt Engineering Guide", "url": "https://www.promptingguide.ai/", "source": "Prompting Guide"},
        {"type": "article", "title": "Anthropic Prompt Library", "url": "https://docs.anthropic.com/claude/prompt-library", "source": "Anthropic"}
      ],
      "practiceExercise": "Create a prompt template for code review. Test it with 3 different code samples. Use few-shot examples and chain-of-thought.",
      "prerequisites": ["phase-4-topic-3"],
      "nextTopics": ["phase-4-topic-5"]
    },
    {
      "id": "phase-4-topic-5",
      "title": "Structured Outputs (JSON Mode)",
      "overview": "Get reliable, parseable JSON responses from LLMs for programmatic use.",
      "estimatedHours": 2,
      "difficulty": "Intermediate",
      "detailedNotes": "# Structured Outputs (JSON Mode)\n\nGet consistent, parseable JSON from LLMs.\n\n## Why Structured Outputs?\n\n- **Reliability**: Parse responses programmatically\n- **Validation**: Ensure correct format\n- **Integration**: Feed into other systems\n\n## Requesting JSON\n\n```python\nprompt = \"\"\"Extract information and return as JSON:\n\nText: \"John Smith works at Acme Corp as a Software Engineer. Email: john@acme.com\"\n\nReturn JSON with keys: name, company, role, email\"\"\"\n\nmessage = client.messages.create(\n    model=\"claude-3-5-sonnet-20241022\",\n    max_tokens=1024,\n    messages=[{\"role\": \"user\", \"content\": prompt}]\n)\n\nimport json\nresult = json.loads(message.content[0].text)\nprint(result['name'])  # \"John Smith\"\n```\n\n## JSON Schema\n\n```python\nprompt = f\"\"\"Extract data and return JSON matching this schema:\n\n{{\n  \"name\": \"string\",\n  \"age\": \"number\",\n  \"email\": \"string\",\n  \"skills\": [\"string\"]\n}}\n\nText: \"Sarah, 28, sarah@example.com, knows Python and JavaScript\"\"\"\"\n```\n\n## Validation\n\n```python\nimport json\nfrom jsonschema import validate, ValidationError\n\nschema = {\n    \"type\": \"object\",\n    \"properties\": {\n        \"name\": {\"type\": \"string\"},\n        \"age\": {\"type\": \"number\"},\n        \"email\": {\"type\": \"string\", \"format\": \"email\"}\n    },\n    \"required\": [\"name\", \"email\"]\n}\n\ndef get_structured_output(prompt, schema):\n    message = client.messages.create(\n        model=\"claude-3-5-sonnet-20241022\",\n        max_tokens=1024,\n        messages=[{\"role\": \"user\", \"content\": prompt}]\n    )\n    \n    try:\n        result = json.loads(message.content[0].text)\n        validate(instance=result, schema=schema)\n        return result\n    except (json.JSONDecodeError, ValidationError) as e:\n        print(f\"Invalid output: {e}\")\n        return None\n```\n\n## Retry Logic\n\n```python\ndef get_json_with_retry(prompt, max_retries=3):\n    for attempt in range(max_retries):\n        message = client.messages.create(\n            model=\"claude-3-5-sonnet-20241022\",\n            max_tokens=1024,\n            messages=[{\"role\": \"user\", \"content\": prompt}]\n        )\n        \n        try:\n            return json.loads(message.content[0].text)\n        except json.JSONDecodeError:\n            if attempt == max_retries - 1:\n                raise\n            prompt += \"\\n\\nPlease return ONLY valid JSON, no other text.\"\n    \n    return None\n```\n\n## Pydantic Models\n\n```python\nfrom pydantic import BaseModel, EmailStr\nfrom typing import List\n\nclass Person(BaseModel):\n    name: str\n    age: int\n    email: EmailStr\n    skills: List[str]\n\ndef extract_person(text):\n    prompt = f\"\"\"Extract person info from text and return JSON:\n    {text}\n    \n    Format:\n    {{\n      \"name\": \"string\",\n      \"age\": number,\n      \"email\": \"string\",\n      \"skills\": [\"string\"]\n    }}\"\"\"\n    \n    message = client.messages.create(\n        model=\"claude-3-5-sonnet-20241022\",\n        max_tokens=1024,\n        messages=[{\"role\": \"user\", \"content\": prompt}]\n    )\n    \n    data = json.loads(message.content[0].text)\n    return Person(**data)  # Validates automatically\n\nperson = extract_person(\"John, 30, john@example.com, Python expert\")\nprint(person.name)  # Type-safe access\n```\n\n## Best Practices\n\n1. Provide schema - show exact format\n2. Validate output - don't trust blindly\n3. Handle errors - retry or fallback\n4. Use Pydantic - type safety\n5. Test edge cases - empty, invalid data",
      "keyConcepts": ["JSON output", "Schema validation", "Pydantic models", "Retry logic", "Error handling"],
      "resources": [
        {"type": "article", "title": "JSON Schema", "url": "https://json-schema.org/", "source": "JSON Schema"},
        {"type": "article", "title": "Pydantic Documentation", "url": "https://docs.pydantic.dev/", "source": "Pydantic"}
      ],
      "practiceExercise": "Build a function that extracts structured data from text (name, email, skills) and validates it using Pydantic. Handle invalid JSON gracefully.",
      "prerequisites": ["phase-4-topic-4"],
      "nextTopics": ["phase-4-topic-6"]
    },
    {
      "id": "phase-4-topic-6",
      "title": "Streaming Responses",
      "overview": "Implement real-time token streaming for better user experience in chat applications.",
      "estimatedHours": 2,
      "difficulty": "Intermediate",
      "detailedNotes": "# Streaming Responses\n\nDisplay tokens as they're generated for better UX.\n\n## Why Stream?\n\n- **Better UX**: Users see progress immediately\n- **Perceived speed**: Feels faster\n- **Early cancellation**: Stop if going wrong\n\n## Claude Streaming\n\n```python\nwith client.messages.stream(\n    model=\"claude-3-5-sonnet-20241022\",\n    max_tokens=1024,\n    messages=[{\"role\": \"user\", \"content\": \"Write a story\"}]\n) as stream:\n    for text in stream.text_stream:\n        print(text, end=\"\", flush=True)\n```\n\n## OpenAI Streaming\n\n```python\nstream = client.chat.completions.create(\n    model=\"gpt-4\",\n    messages=[{\"role\": \"user\", \"content\": \"Write a story\"}],\n    stream=True\n)\n\nfor chunk in stream:\n    if chunk.choices[0].delta.content:\n        print(chunk.choices[0].delta.content, end=\"\", flush=True)\n```\n\n## Collecting Full Response\n\n```python\ndef stream_and_collect(prompt):\n    full_response = \"\"\n    \n    with client.messages.stream(\n        model=\"claude-3-5-sonnet-20241022\",\n        max_tokens=1024,\n        messages=[{\"role\": \"user\", \"content\": prompt}]\n    ) as stream:\n        for text in stream.text_stream:\n            print(text, end=\"\", flush=True)\n            full_response += text\n    \n    print()  # New line\n    return full_response\n```\n\n## Streaming with Callbacks\n\n```python\ndef stream_with_callback(prompt, on_token=None, on_complete=None):\n    full_response = \"\"\n    \n    with client.messages.stream(\n        model=\"claude-3-5-sonnet-20241022\",\n        max_tokens=1024,\n        messages=[{\"role\": \"user\", \"content\": prompt}]\n    ) as stream:\n        for text in stream.text_stream:\n            full_response += text\n            if on_token:\n                on_token(text)\n    \n    if on_complete:\n        on_complete(full_response)\n    \n    return full_response\n\n# Usage\ndef print_token(token):\n    print(token, end=\"\", flush=True)\n\ndef on_done(response):\n    print(f\"\\n\\nTotal length: {len(response)}\")\n\nstream_with_callback(\n    \"Write a haiku\",\n    on_token=print_token,\n    on_complete=on_done\n)\n```\n\n## Error Handling\n\n```python\ntry:\n    with client.messages.stream(\n        model=\"claude-3-5-sonnet-20241022\",\n        max_tokens=1024,\n        messages=[{\"role\": \"user\", \"content\": prompt}]\n    ) as stream:\n        for text in stream.text_stream:\n            print(text, end=\"\", flush=True)\nexcept Exception as e:\n    print(f\"\\nStreaming error: {e}\")\n```\n\n## Best Practices\n\n1. Always flush - `flush=True` for real-time display\n2. Handle errors - stream can fail mid-way\n3. Collect full response - for logging/storage\n4. Use callbacks - flexible handling\n5. Test cancellation - user might stop early",
      "keyConcepts": ["Streaming API", "Real-time display", "Callbacks", "Error handling", "Web streaming"],
      "resources": [
        {"type": "article", "title": "Anthropic Streaming", "url": "https://docs.anthropic.com/claude/reference/streaming", "source": "Anthropic"}
      ],
      "practiceExercise": "Build a streaming chatbot that displays tokens in real-time. Add a callback to count tokens as they arrive.",
      "prerequisites": ["phase-4-topic-3"],
      "nextTopics": ["phase-4-topic-7"]
    },
    {
      "id": "phase-4-topic-7",
      "title": "Token Economics",
      "overview": "Understand tokens, estimate costs, manage context windows, and choose the right model.",
      "estimatedHours": 2,
      "difficulty": "Intermediate",
      "detailedNotes": "# Token Economics\n\nTokens = money. Understand and optimize.\n\n## What are Tokens?\n\nTokens are pieces of text. Roughly:\n- 1 token ≈ 4 characters\n- 1 token ≈ 0.75 words\n- 100 tokens ≈ 75 words\n\n## Counting Tokens\n\n```python\nimport tiktoken\n\n# For OpenAI\nencoding = tiktoken.encoding_for_model(\"gpt-4\")\ntext = \"Hello, how are you?\"\ntokens = encoding.encode(text)\nprint(f\"Tokens: {len(tokens)}\")  # ~5 tokens\n\n# For Claude (approximate)\ndef estimate_tokens(text):\n    return len(text) // 4\n```\n\n## Pricing (2024)\n\n**Claude:**\n- Haiku: $0.25/M input, $1.25/M output\n- Sonnet: $3/M input, $15/M output\n- Opus: $15/M input, $75/M output\n\n**OpenAI:**\n- GPT-3.5: $0.50/M input, $1.50/M output\n- GPT-4: $30/M input, $60/M output\n- GPT-4 Turbo: $10/M input, $30/M output\n\n## Cost Calculation\n\n```python\ndef calculate_cost(input_tokens, output_tokens, model=\"claude-sonnet\"):\n    pricing = {\n        \"claude-haiku\": (0.25, 1.25),\n        \"claude-sonnet\": (3, 15),\n        \"claude-opus\": (15, 75),\n        \"gpt-3.5\": (0.5, 1.5),\n        \"gpt-4\": (30, 60)\n    }\n    \n    input_price, output_price = pricing[model]\n    input_cost = (input_tokens / 1_000_000) * input_price\n    output_cost = (output_tokens / 1_000_000) * output_price\n    \n    return input_cost + output_cost\n\ncost = calculate_cost(1000, 500, \"claude-sonnet\")\nprint(f\"Cost: ${cost:.4f}\")  # $0.0105\n```\n\n## Context Windows\n\n**Model limits:**\n- Claude Haiku/Sonnet/Opus: 200K tokens\n- GPT-3.5: 16K tokens\n- GPT-4: 8K-128K tokens\n\n## Choosing the Right Model\n\n**Use Haiku when:** Simple tasks, high volume, cost-sensitive, fast response needed\n\n**Use Sonnet when:** Balanced performance/cost, most use cases, good reasoning needed\n\n**Use Opus when:** Complex reasoning, highest quality needed, cost less important\n\n## Optimization Strategies\n\n**1. Trim Context:**\n```python\ndef trim_messages(messages, max_tokens=4000):\n    if len(messages) <= 2:\n        return messages\n    \n    total = sum(len(m['content']) // 4 for m in messages)\n    \n    if total <= max_tokens:\n        return messages\n    \n    return [messages[0]] + messages[-(max_tokens // 500):]\n```\n\n**2. Use Cheaper Models:**\n```python\ndef smart_model_selection(task_complexity):\n    if task_complexity == \"simple\":\n        return \"claude-3-haiku-20240307\"\n    elif task_complexity == \"medium\":\n        return \"claude-3-5-sonnet-20241022\"\n    else:\n        return \"claude-3-opus-20240229\"\n```\n\n## Tracking Usage\n\n```python\nclass UsageTracker:\n    def __init__(self):\n        self.total_input_tokens = 0\n        self.total_output_tokens = 0\n    \n    def track(self, response):\n        usage = response.usage\n        self.total_input_tokens += usage.input_tokens\n        self.total_output_tokens += usage.output_tokens\n    \n    def get_cost(self, model=\"claude-sonnet\"):\n        return calculate_cost(\n            self.total_input_tokens,\n            self.total_output_tokens,\n            model\n        )\n\ntracker = UsageTracker()\nmessage = client.messages.create(...)\ntracker.track(message)\nprint(f\"Total cost: ${tracker.get_cost():.4f}\")\n```\n\n## Best Practices\n\n1. Monitor usage - track tokens and costs\n2. Choose wisely - right model for task\n3. Trim context - don't send unnecessary history\n4. Cache prompts - reuse system prompts\n5. Set limits - max tokens per request\n6. Test costs - calculate before scaling",
      "keyConcepts": ["Tokens", "Pricing", "Context windows", "Model selection", "Cost optimization", "Usage tracking"],
      "resources": [
        {"type": "article", "title": "Anthropic Pricing", "url": "https://www.anthropic.com/pricing", "source": "Anthropic"},
        {"type": "article", "title": "OpenAI Pricing", "url": "https://openai.com/pricing", "source": "OpenAI"}
      ],
      "practiceExercise": "Build a usage tracker that logs tokens and calculates costs for each API call. Compare costs between different models for the same task.",
      "prerequisites": ["phase-4-topic-1"],
      "nextTopics": ["phase-4-topic-8"]
    },
    {
      "id": "phase-4-topic-8",
      "title": "Robust Error Handling",
      "overview": "Handle rate limits, implement exponential backoff, and manage API errors gracefully.",
      "estimatedHours": 2,
      "difficulty": "Intermediate",
      "detailedNotes": "# Robust Error Handling\n\nAPIs fail. Handle errors gracefully.\n\n## Common API Errors\n\n**Rate Limits (429):**\n```python\nfrom anthropic import RateLimitError\n\ntry:\n    message = client.messages.create(...)\nexcept RateLimitError as e:\n    print(\"Rate limited. Wait and retry.\")\n```\n\n**Authentication (401):**\n```python\nfrom anthropic import AuthenticationError\n\ntry:\n    message = client.messages.create(...)\nexcept AuthenticationError:\n    print(\"Invalid API key\")\n```\n\n**Invalid Request (400):**\n```python\nfrom anthropic import BadRequestError\n\ntry:\n    message = client.messages.create(...)\nexcept BadRequestError as e:\n    print(f\"Invalid request: {e}\")\n```\n\n## Exponential Backoff\n\n```python\nimport time\nimport random\n\ndef call_with_backoff(func, max_retries=5):\n    for attempt in range(max_retries):\n        try:\n            return func()\n        except RateLimitError:\n            if attempt == max_retries - 1:\n                raise\n            \n            wait_time = (2 ** attempt) + random.uniform(0, 1)\n            print(f\"Rate limited. Waiting {wait_time:.1f}s...\")\n            time.sleep(wait_time)\n    \n    raise Exception(\"Max retries exceeded\")\n\ndef make_request():\n    return client.messages.create(\n        model=\"claude-3-5-sonnet-20241022\",\n        max_tokens=1024,\n        messages=[{\"role\": \"user\", \"content\": \"Hello\"}]\n    )\n\nresponse = call_with_backoff(make_request)\n```\n\n## Comprehensive Error Handler\n\n```python\nfrom anthropic import (\n    APIError,\n    RateLimitError,\n    AuthenticationError,\n    BadRequestError,\n    APIConnectionError,\n    APITimeoutError\n)\nimport time\nimport random\n\ndef robust_api_call(prompt, max_retries=3):\n    for attempt in range(max_retries):\n        try:\n            message = client.messages.create(\n                model=\"claude-3-5-sonnet-20241022\",\n                max_tokens=1024,\n                messages=[{\"role\": \"user\", \"content\": prompt}],\n                timeout=30.0\n            )\n            return message.content[0].text\n        \n        except RateLimitError:\n            if attempt == max_retries - 1:\n                raise\n            wait_time = (2 ** attempt) + random.uniform(0, 1)\n            time.sleep(wait_time)\n        \n        except AuthenticationError:\n            raise ValueError(\"Invalid API key\")\n        \n        except BadRequestError as e:\n            raise ValueError(f\"Invalid request: {e}\")\n        \n        except APIConnectionError:\n            if attempt == max_retries - 1:\n                raise\n            time.sleep(2)\n        \n        except APITimeoutError:\n            if attempt == max_retries - 1:\n                raise\n            time.sleep(1)\n        \n        except APIError as e:\n            print(f\"API error: {e}\")\n            raise\n    \n    raise Exception(\"Max retries exceeded\")\n```\n\n## Circuit Breaker Pattern\n\n```python\nfrom datetime import datetime, timedelta\n\nclass CircuitBreaker:\n    def __init__(self, failure_threshold=5, timeout=60):\n        self.failure_threshold = failure_threshold\n        self.timeout = timeout\n        self.failures = 0\n        self.last_failure_time = None\n        self.state = \"closed\"  # closed, open, half-open\n    \n    def call(self, func):\n        if self.state == \"open\":\n            if datetime.now() - self.last_failure_time > timedelta(seconds=self.timeout):\n                self.state = \"half-open\"\n            else:\n                raise Exception(\"Circuit breaker is open\")\n        \n        try:\n            result = func()\n            if self.state == \"half-open\":\n                self.state = \"closed\"\n                self.failures = 0\n            return result\n        except Exception as e:\n            self.failures += 1\n            self.last_failure_time = datetime.now()\n            \n            if self.failures >= self.failure_threshold:\n                self.state = \"open\"\n            \n            raise e\n\nbreaker = CircuitBreaker()\nresponse = breaker.call(make_request)\n```\n\n## Best Practices\n\n1. Use exponential backoff for rate limits\n2. Catch specific exceptions\n3. Set reasonable timeouts\n4. Log errors for debugging\n5. Implement circuit breakers for repeated failures\n6. Provide user-friendly error messages",
      "keyConcepts": ["Rate limits", "Exponential backoff", "Error types", "Circuit breaker", "Retry logic", "Timeout handling"],
      "resources": [
        {"type": "article", "title": "Anthropic Error Handling", "url": "https://docs.anthropic.com/claude/reference/errors", "source": "Anthropic"}
      ],
      "practiceExercise": "Build a robust API wrapper that handles all error types, implements exponential backoff, and includes a circuit breaker. Test with intentional failures.",
      "prerequisites": ["phase-4-topic-1"],
      "nextTopics": ["phase-5-topic-1"]
    }
  ]
}